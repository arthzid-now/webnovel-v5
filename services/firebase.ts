// Firebase service has been removed for now to allow Vercel deployment without configuration.
// When you are ready to add backend features (Cloud Save, Auth), restore this file using the Firebase SDK snippet.
export {};